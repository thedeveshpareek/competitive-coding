#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
	int i=0;
	char data[10];
	while(1)
	{
		char ff[10]="mkdir ";
		sprintf(data,"%d",i);
		strcat(ff, data);
		system(ff);
		i++;
	}
	return 0;
}
